import checkbox from './checkbox';
import singleSelect from './singleSelect';
import table from './table';

const coreDefaultTheme = {
    checkbox,
    table,
    singleSelect
};

export default coreDefaultTheme;
