import { Color, ColorPallet as MedlyColorPallet } from '@medly-components/theme';

export interface ColorPallet extends MedlyColorPallet {
    purple: Color;
    coral: Color;
    lime: Color;
    forest: Color;
}
